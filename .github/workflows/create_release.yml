name: create release

on:
  workflow_dispatch:

jobs:
  create:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:

      - name: Checkout
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: main

      - name: LastRelease
        id: last
        env: 
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          last_tag=`gh release view --json tagName --jq .tagName`
          echo $last_tag
          echo "last_tag=$last_tag" >> "$GITHUB_OUTPUT"
          echo "repo=${GITHUB_REPOSITORY#${GITHUB_REPOSITORY_OWNER}/}" >> $GITHUB_OUTPUT

      - name : Setup Deno
        uses: denoland/setup-deno@v1
        with:
          deno-version: v1.x
          
      - name : Run Deno
        env: 
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          LAST_TAG: ${{ steps.last.outputs.last_tag }}
          OWNER: ${{ github.repository_owner }}
          REPO: ${{ steps.last.outputs.repo }}
        run: |
          result=`deno run --allow-read --allow-env --allow-net .deno/release.ts`
          echo $result
        







# 最後のリリースのタグを取得
# 作成されるであろうリリースノートを取得
# 内容を元にセマンティックバージョンを上げる
# もう一度リリースノートを作成してCHANGELOGに追記
# READMEとCHAGELOGをコピー
# コミットする
# タグを付ける
# プッシュ
# タグでリリースを作成
